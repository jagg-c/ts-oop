import { Animal, Dog } from './09-protected';

// const animal = new Animal('elite'); // Can't implement super class, only subclasses
// animal.greeting();

const cheis = new Dog('cheis', 'nico');
cheis.move();
